# Real-world clips

Openly licensed real clips used for the small real-world evaluation set live here
(not committed). Every file is listed in `data/SOURCES.md` with its origin and licence.
Fetch them with `make real` (see `synth/fetch_real.py`).
