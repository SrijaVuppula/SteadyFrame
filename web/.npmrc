# chromedriver downloads its binary in a postinstall from googlechromelabs.github.io, which
# is blocked on some networks. Skip it; scripts/a11y.mjs resolves a driver at run time.
chromedriver_skip_download=true
